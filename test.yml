parameters:
- name: resourceGroup
  type: string
  default: 'invalid resourceGroup'
- name: connectionName
  type: string
  default: 'invalid connectionName'
- name: subscriptionID
  type: string
  default: 'invalid subscriptionID'
- name: azureDeployParameters
  type: string
  default: 'invalid azureDeployParameters'
- name: webAppName
  type: string
  default: 'invalid webAppName'
- name: cloudflareapiuser
  type: string
  default: 'invalid cloudflareapiuser'
- name: cloudflareapikey
  type: string
  default: 'invalid cloudflareapikey'
- name: hostname
  type: string
  default: 'invalid hostname'

steps:
- script: echo ${{ parameters.resourceGroup }}
- script: echo ${{ parameters.connectionName }}
- script: echo ${{ parameters.subscriptionID }}
- script: echo ${{ parameters.azureDeployParameters }}
- script: echo ${{ parameters.webAppName }}
- script: echo ${{ parameters.cloudflareapiuser }}
- script: echo ${{ parameters.cloudflareapikey }}
- script: echo ${{ parameters.hostname }}
- script: more D:\a\_temp\bc47b08c-be72-4d69-8275-eb91b9fd4ccb.cmd